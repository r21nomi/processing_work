box: debian
build:
    steps:
        - arjen/hugo-build:
            version: 0.14
            theme: custom
            flags: --buildDrafts=true
deploy:
    steps:
        - lukevivier/gh-pages@0.2.1:
            token: $GIT_TOKEN
            repo: processing_work/nomi1126.github.io
            domain: github.com
            basedir: public
